#compdef claude_session cs

local -a cmds
cmds=(
  'save:アクティブなセッション一覧を保存する(再起動前に実行)'
  'list:保存済みセッションを cwd に関係なく一覧表示(パイプ時は生 TSV)'
  'select:stdin の候補から1行選んでそのまま出力する(フィルタ)'
  'resume:cwd のセッションを1つ選び claude --resume する'
)
(( CURRENT == 2 )) && _describe 'command' cmds
